// Custom scalar and aggregate UDFs for OpenFoundry.
//
// Future: geospatial functions, fuzzy matching, ontology lookups.
//
// Mirrors `libs/query-engine/src/udf.rs` — kept as a doc-only stub
// on the Go side so the module layout is parallel to the Rust crate
// and future implementations have an obvious home.
package queryengine
