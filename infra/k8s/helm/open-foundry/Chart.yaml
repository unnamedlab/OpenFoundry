apiVersion: v2
name: open-foundry
description: OpenFoundry platform chart with autoscaling-aware service deployments
type: application
version: 0.1.0
appVersion: "0.1.0"

dependencies:
  - name: iceberg-catalog
    alias: icebergCatalog
    version: 0.1.0
    repository: ""
    condition: icebergCatalog.enabled
