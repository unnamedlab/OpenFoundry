No active SQL migrations remain in this directory.

The legacy Postgres queue tables for `automation-operations-service`
were archived under:

- `docs/architecture/legacy-migrations/automation-operations-service/`

Temporal is now the authoritative runtime for automation operations.
