x-common: &common
  restart: unless-stopped
  logging:
    driver: json-file
    options:
      max-size: "10m"
      max-file: "3"

x-app-common: &app-common
  <<: *common
  profiles: ["app"]

# Cumulative rollout profiles:
# foundation -> data -> knowledge -> intelligence -> experience -> edge
# Each later profile includes all earlier application services.
x-profiles-foundation: &profiles-foundation ["app", "foundation", "data", "knowledge", "intelligence", "experience", "edge"]
x-profiles-data: &profiles-data ["app", "data", "knowledge", "intelligence", "experience", "edge"]
x-profiles-knowledge: &profiles-knowledge ["app", "knowledge", "intelligence", "experience", "edge"]
x-profiles-intelligence: &profiles-intelligence ["app", "intelligence", "experience", "edge"]
x-profiles-experience: &profiles-experience ["app", "experience", "edge"]
x-profiles-edge: &profiles-edge ["app", "edge"]

services:
  # ── PostgreSQL ──────────────────────────────────────────────
  postgres:
    <<: *common
    image: ${OPENFOUNDRY_POSTGRES_IMAGE:-postgres:16-alpine}
    ports:
      - "${OPENFOUNDRY_POSTGRES_HOST_PORT:-5432}:5432"
    environment:
      POSTGRES_USER: openfoundry
      POSTGRES_PASSWORD: openfoundry
      POSTGRES_DB: openfoundry
      # Comma-separated extra databases created by ./init-db/01-create-databases.sh.
      # Override (extend) in `docker-compose.dev.yml` for full per-service split.
      # `pg_policy` is included by default because the transactional outbox
      # (ADR-0022) lives in `pg_policy.outbox.events` and is mirrored to Kafka
      # by the Debezium Connect service below.
      POSTGRES_MULTIPLE_DATABASES: ${OPENFOUNDRY_POSTGRES_EXTRA_DATABASES:-pg_policy}
    # WAL logical decoding is required by the Debezium Postgres connector
    # (ADR-0022). `max_replication_slots`/`max_wal_senders` are sized for
    # one Debezium task per logical DB plus headroom; bump if you add more.
    command:
      - postgres
      - -c
      - wal_level=logical
      - -c
      - max_replication_slots=4
      - -c
      - max_wal_senders=4
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init-db:/docker-entrypoint-initdb.d
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U openfoundry"]
      interval: 5s
      timeout: 5s
      retries: 5

  # ── Valkey (OSS Redis-compatible store, BSD-3) ─────────────
  valkey:
    <<: *common
    image: ${OPENFOUNDRY_VALKEY_IMAGE:-valkey/valkey:8-alpine}
    ports:
      - "${OPENFOUNDRY_REDIS_HOST_PORT:-6379}:6379"
    command: valkey-server --maxmemory 256mb --maxmemory-policy allkeys-lru
    volumes:
      - valkey_data:/data
    healthcheck:
      test: ["CMD", "valkey-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

  # ── NATS (JetStream) ───────────────────────────────────────
  nats:
    <<: *common
    image: ${OPENFOUNDRY_NATS_IMAGE:-nats:2-alpine}
    ports:
      - "${OPENFOUNDRY_NATS_HOST_PORT:-4222}:4222"
      - "${OPENFOUNDRY_NATS_MONITOR_HOST_PORT:-8222}:8222"
    command: --jetstream --store_dir /data --http_port 8222
    volumes:
      - nats_data:/data
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:8222/healthz"]
      interval: 5s
      timeout: 5s
      retries: 5

  # ── MinIO (S3-compatible) ──────────────────────────────────
  minio:
    <<: *common
    image: ${OPENFOUNDRY_MINIO_IMAGE:-minio/minio:latest}
    ports:
      - "${OPENFOUNDRY_MINIO_API_HOST_PORT:-9000}:9000"
      - "${OPENFOUNDRY_MINIO_CONSOLE_HOST_PORT:-9001}:9001"
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    command: server /data --console-address ":9001"
    volumes:
      - minio_data:/data
    healthcheck:
      test: ["CMD", "mc", "ready", "local"]
      interval: 5s
      timeout: 5s
      retries: 5

  minio-init:
    image: ${OPENFOUNDRY_MINIO_MC_IMAGE:-minio/mc:latest}
    depends_on:
      minio:
        condition: service_healthy
    entrypoint: >
      /bin/sh -c "
      mc alias set local http://minio:9000 minioadmin minioadmin &&
      mc mb --ignore-existing local/openfoundry &&
      mc mb --ignore-existing local/openfoundry-datasets &&
      mc mb --ignore-existing local/openfoundry-notebooks &&
      mc mb --ignore-existing local/openfoundry-artifacts
      "

  # ── Vespa Lite (DX search engine, Apache-2.0) ──────────────
  # Single-node container of the same Vespa engine that runs in production
  # (see infra/runbooks/vespa.md and docs/architecture/adr/ADR-0007-search-engine-choice.md).
  # Exposes the config server on 19071 (deploy endpoint) and the container
  # cluster on 8080 (query/feed); the latter is remapped to host port
  # ${OPENFOUNDRY_VESPA_QUERY_HOST_PORT:-18080} to avoid clashing with the
  # API gateway. Use this for hybrid (BM25 + vector + filters + ranking)
  # search during local development; pgvector still covers the embedded
  # vector case.
  #
  # Meilisearch is no longer part of the default DX stack; it lives under
  # the optional `--profile demo` in infra/docker-compose.dev.yml for
  # first-run demos only (no production code or test depends on it).
  vespa:
    <<: *common
    image: ${OPENFOUNDRY_VESPA_IMAGE:-vespaengine/vespa:8.450.30}
    hostname: vespa
    ports:
      - "${OPENFOUNDRY_VESPA_QUERY_HOST_PORT:-18080}:8080"
      - "${OPENFOUNDRY_VESPA_CONFIG_HOST_PORT:-19071}:19071"
    volumes:
      - vespa_data:/opt/vespa/var
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:19071/state/v1/health"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 60s

  # Qdrant se retiró por restricción de licencia OSS; fue sustituido por
  # Vespa (Apache-2.0). pgvector cubre el caso embebido.

  # ── Cassandra 5 (single-node DX) ────────────────────────
  # Single-node Cassandra 5 for local development. Production runs the
  # full 3-DC × 3-rack × RF=3 layout via k8ssandra-operator (see
  # `infra/k8s/cassandra/` and `infra/runbooks/cassandra.md`); here we
  # use one node with `SimpleSnitch`-grade overrides because dev work
  # never validates replica placement.
  cassandra:
    <<: *common
    image: ${OPENFOUNDRY_CASSANDRA_IMAGE:-cassandra:5.0}
    hostname: cassandra
    ports:
      - "${OPENFOUNDRY_CASSANDRA_HOST_PORT:-9042}:9042"
    environment:
      CASSANDRA_CLUSTER_NAME: of-dev
      CASSANDRA_DC: dc1
      CASSANDRA_RACK: rack1
      CASSANDRA_ENDPOINT_SNITCH: GossipingPropertyFileSnitch
      HEAP_NEWSIZE: 128M
      MAX_HEAP_SIZE: 1G
    volumes:
      - cassandra_data:/var/lib/cassandra
    healthcheck:
      test: ["CMD-SHELL", "cqlsh -e 'DESCRIBE KEYSPACES'"]
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 60s

  # Init container that creates the 6 application keyspaces with RF=1.
  # Mirrors `infra/k8s/cassandra/keyspaces-job.yaml` but with RF=1 since
  # this is a single-node cluster. The `temporal_persistence` and
  # `temporal_visibility` keyspaces are created by the Temporal
  # auto-setup container itself, so we do not duplicate them here.
  cassandra-init:
    image: ${OPENFOUNDRY_CASSANDRA_IMAGE:-cassandra:5.0}
    depends_on:
      cassandra:
        condition: service_healthy
    entrypoint:
      - /bin/sh
      - -c
      - |
        set -e
        cqlsh cassandra 9042 -e "
          CREATE KEYSPACE IF NOT EXISTS ontology_objects     WITH replication = {'class':'NetworkTopologyStrategy','dc1':'1'};
          CREATE KEYSPACE IF NOT EXISTS ontology_indexes     WITH replication = {'class':'NetworkTopologyStrategy','dc1':'1'};
          CREATE KEYSPACE IF NOT EXISTS actions_log          WITH replication = {'class':'NetworkTopologyStrategy','dc1':'1'};
          CREATE KEYSPACE IF NOT EXISTS auth_runtime         WITH replication = {'class':'NetworkTopologyStrategy','dc1':'1'};
          CREATE KEYSPACE IF NOT EXISTS notifications_inbox  WITH replication = {'class':'NetworkTopologyStrategy','dc1':'1'};
          CREATE KEYSPACE IF NOT EXISTS agent_state          WITH replication = {'class':'NetworkTopologyStrategy','dc1':'1'};
          DESCRIBE KEYSPACES;
        "
    restart: "no"

  # ── Temporal (devserver) ──────────────────────────────
  # `temporalio/auto-setup:1.24` (the all-in-one image) backed by the
  # Cassandra container above. Production also runs Temporal on
  # Cassandra (ADR-0021); using the same backend in dev keeps the
  # surface identical and exercises the keyspace creation path.
  temporal:
    <<: *common
    image: ${OPENFOUNDRY_TEMPORAL_IMAGE:-temporalio/auto-setup:1.24}
    ports:
      - "${OPENFOUNDRY_TEMPORAL_FRONTEND_HOST_PORT:-7233}:7233"
    environment:
      DB: cassandra
      CASSANDRA_SEEDS: cassandra
      CASSANDRA_PORT: "9042"
      KEYSPACE: temporal_persistence
      VISIBILITY_KEYSPACE: temporal_visibility
      DEFAULT_NAMESPACE: default
      DEFAULT_NAMESPACE_RETENTION: 1d
      SKIP_DEFAULT_NAMESPACE_CREATION: "false"
    depends_on:
      cassandra-init:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD-SHELL", "tctl --address 127.0.0.1:7233 cluster health || temporal operator cluster health"]
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 60s

  # Temporal Web UI bound to the dev frontend.
  temporal-ui:
    <<: *common
    image: ${OPENFOUNDRY_TEMPORAL_UI_IMAGE:-temporalio/ui:2.31.2}
    ports:
      - "${OPENFOUNDRY_TEMPORAL_UI_HOST_PORT:-8233}:8080"
    environment:
      TEMPORAL_ADDRESS: temporal:7233
      TEMPORAL_CORS_ORIGINS: http://localhost:3000
    depends_on:
      temporal:
        condition: service_started

  # ── OpenSearch (dev/CI search backend) ────────────────────
  # Per ADR-0028, OpenSearch is the dev / CI fallback for the Vespa
  # production search backend. Single-node, security plugin disabled,
  # heap capped at ~512 MB so the whole compose stack still fits in
  # ~6–7 GB on a developer laptop.
  opensearch:
    <<: *common
    image: ${OPENFOUNDRY_OPENSEARCH_IMAGE:-opensearchproject/opensearch:2.18.0}
    hostname: opensearch
    ports:
      - "${OPENFOUNDRY_OPENSEARCH_HOST_PORT:-9200}:9200"
      - "${OPENFOUNDRY_OPENSEARCH_PERF_HOST_PORT:-9600}:9600"
    environment:
      discovery.type: single-node
      DISABLE_SECURITY_PLUGIN: "true"
      DISABLE_INSTALL_DEMO_CONFIG: "true"
      bootstrap.memory_lock: "true"
      OPENSEARCH_JAVA_OPTS: "-Xms512m -Xmx512m"
    ulimits:
      memlock:
        soft: -1
        hard: -1
      nofile:
        soft: 65536
        hard: 65536
    volumes:
      - opensearch_data:/usr/share/opensearch/data
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://localhost:9200/_cluster/health | grep -E '\"status\":\"(green|yellow)\"'"]
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 30s

  # ── Kafka (KRaft, single-broker) ───────────────────────
  # Required by Debezium Connect to publish CDC events from the
  # `pg_policy.outbox.events` table (ADR-0022). KRaft mode (no
  # ZooKeeper) keeps the dev footprint to a single container.
  kafka:
    <<: *common
    image: ${OPENFOUNDRY_KAFKA_IMAGE:-apache/kafka:3.7.1}
    hostname: kafka
    ports:
      - "${OPENFOUNDRY_KAFKA_HOST_PORT:-9092}:9092"
    environment:
      KAFKA_NODE_ID: "1"
      KAFKA_PROCESS_ROLES: broker,controller
      KAFKA_LISTENERS: PLAINTEXT://0.0.0.0:9092,CONTROLLER://0.0.0.0:9093
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:9092
      KAFKA_CONTROLLER_LISTENER_NAMES: CONTROLLER
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT
      KAFKA_CONTROLLER_QUORUM_VOTERS: 1@kafka:9093
      KAFKA_INTER_BROKER_LISTENER_NAME: PLAINTEXT
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: "1"
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: "1"
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: "1"
      KAFKA_AUTO_CREATE_TOPICS_ENABLE: "true"
      CLUSTER_ID: ${OPENFOUNDRY_KAFKA_CLUSTER_ID:-of-dev-kafka-cluster-1}
    volumes:
      - kafka_data:/var/lib/kafka/data
    healthcheck:
      test: ["CMD-SHELL", "/opt/kafka/bin/kafka-topics.sh --bootstrap-server localhost:9092 --list >/dev/null 2>&1"]
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 30s

  # ── Debezium Kafka Connect (Postgres CDC) ─────────────────
  # Implements the relay side of ADR-0022 (Postgres outbox → Kafka).
  # The connector itself is registered by `debezium-connect-init`
  # below so this container stays a vanilla Kafka Connect distribution
  # whose lifecycle does not depend on the connector definition.
  debezium-connect:
    <<: *common
    image: ${OPENFOUNDRY_DEBEZIUM_IMAGE:-debezium/connect:2.7}
    ports:
      - "${OPENFOUNDRY_DEBEZIUM_HOST_PORT:-8083}:8083"
    environment:
      BOOTSTRAP_SERVERS: kafka:9092
      GROUP_ID: of-debezium-connect
      CONFIG_STORAGE_TOPIC: of_connect_configs
      OFFSET_STORAGE_TOPIC: of_connect_offsets
      STATUS_STORAGE_TOPIC: of_connect_statuses
      CONFIG_STORAGE_REPLICATION_FACTOR: "1"
      OFFSET_STORAGE_REPLICATION_FACTOR: "1"
      STATUS_STORAGE_REPLICATION_FACTOR: "1"
      KEY_CONVERTER: org.apache.kafka.connect.json.JsonConverter
      VALUE_CONVERTER: org.apache.kafka.connect.json.JsonConverter
    depends_on:
      kafka:
        condition: service_healthy
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://localhost:8083/ >/dev/null"]
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 30s

  # Registers the `pg_policy.outbox.events` Debezium connector once
  # Connect is up. Idempotent: `PUT` against `/connectors/<name>/config`
  # creates or updates the connector. The `publication.autocreate.mode`
  # is `filtered` so Debezium only creates a publication for the table
  # we actually CDC.
  #
  # The `transforms.outbox.*` block wires the Debezium **EventRouter**
  # SMT (S0.9.c): each WAL record is converted into a Kafka message
  # whose topic is taken from the `topic` column, whose key is the
  # `aggregate_id`, and whose headers are populated from the `headers`
  # JSONB column (notably the `ol-*` OpenLineage trio). The producer
  # side deletes the row inside the same transaction
  # (`libs/outbox/src/lib.rs::enqueue`); the EventRouter only emits on
  # INSERT so the downstream Kafka topic stays clean and the table
  # stays empty without a janitor — this is the canonical Debezium
  # outbox pattern. The plan refers to this behaviour as
  # `outbox.event.deletion.policy=delete`; that exact key is not part
  # of the upstream connector schema, the in-transaction DELETE is the
  # operative mechanism.
  debezium-connect-init:
    image: curlimages/curl:8.10.1
    depends_on:
      debezium-connect:
        condition: service_healthy
    restart: "no"
    entrypoint:
      - /bin/sh
      - -c
      - |
        set -e
        echo "Registering Debezium connector pg-policy-outbox…"
        curl -fsS -X PUT \
          -H 'Content-Type: application/json' \
          --data '{
            "connector.class": "io.debezium.connector.postgresql.PostgresConnector",
            "tasks.max": "1",
            "database.hostname": "postgres",
            "database.port": "5432",
            "database.user": "openfoundry",
            "database.password": "openfoundry",
            "database.dbname": "pg_policy",
            "topic.prefix": "openfoundry.pg_policy",
            "table.include.list": "outbox.events",
            "plugin.name": "pgoutput",
            "slot.name": "of_outbox_slot",
            "publication.name": "of_outbox_pub",
            "publication.autocreate.mode": "filtered",
            "snapshot.mode": "initial",
            "tombstones.on.delete": "false",
            "key.converter": "org.apache.kafka.connect.json.JsonConverter",
            "value.converter": "org.apache.kafka.connect.json.JsonConverter",
            "key.converter.schemas.enable": "false",
            "value.converter.schemas.enable": "false",
            "transforms": "outbox",
            "transforms.outbox.type": "io.debezium.transforms.outbox.EventRouter",
            "transforms.outbox.table.field.event.id": "event_id",
            "transforms.outbox.table.field.event.key": "aggregate_id",
            "transforms.outbox.table.field.event.payload": "payload",
            "transforms.outbox.table.field.event.timestamp": "created_at",
            "transforms.outbox.table.expand.json.payload": "true",
            "transforms.outbox.route.by.field": "topic",
            "transforms.outbox.route.topic.replacement": "${routedByValue}",
            "transforms.outbox.table.fields.additional.placement": "aggregate:header:aggregateType,event_id:header:id",
            "transforms.outbox.headers.json.fields.placement": "headers"
          }' \
          http://debezium-connect:8083/connectors/pg-policy-outbox/config
        echo "Done."

  # ── Apicurio Registry (Schema Registry) ────────────────────
  # ADR-0022 §3.6 — schemas of every event published through the
  # outbox are registered in Apicurio (group `openfoundry`,
  # artifact id = Kafka topic). In dev we run the in-memory variant
  # (`apicurio/apicurio-registry-mem:2.6`) so the registry has zero
  # external dependencies; production uses the same image with the
  # Postgres backend (`apicurio-pg`, see infra/runbooks/kafka.md).
  # The connector still publishes JSON values today so this service
  # is consumed only by producers/consumers that need a schema
  # contract — switching `value.converter` to the Apicurio Avro
  # converter is a follow-up that does not block S0.9.
  apicurio-registry:
    <<: *common
    image: ${OPENFOUNDRY_APICURIO_IMAGE:-apicurio/apicurio-registry-mem:2.6.4.Final}
    ports:
      - "${OPENFOUNDRY_APICURIO_HOST_PORT:-8087}:8080"
    environment:
      QUARKUS_PROFILE: prod
      REGISTRY_AUTH_ENABLED: "false"
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://localhost:8080/health/ready >/dev/null"]
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 20s

  # Registers the JSON Schemas for the outbox event envelope and a
  # representative payload (`ontology.object.changed.v1`) so consumers
  # can validate against a stable contract from day one. Idempotent:
  # `PUT /artifacts/{id}` with `X-Registry-ArtifactId` upserts.
  apicurio-registry-init:
    image: curlimages/curl:8.10.1
    depends_on:
      apicurio-registry:
        condition: service_healthy
    restart: "no"
    entrypoint:
      - /bin/sh
      - -c
      - |
        set -e
        BASE='http://apicurio-registry:8080/apis/registry/v2/groups/openfoundry/artifacts'
        echo "Registering outbox.event.envelope.v1 …"
        curl -fsS -X POST \
          -H 'Content-Type: application/json' \
          -H 'X-Registry-ArtifactId: outbox.event.envelope.v1' \
          -H 'X-Registry-ArtifactType: JSON' \
          --data '{
            "$schema": "http://json-schema.org/draft-07/schema#",
            "title": "OutboxEventEnvelope",
            "type": "object",
            "required": ["event_id", "aggregate", "aggregate_id", "topic", "payload"],
            "properties": {
              "event_id":     {"type": "string", "format": "uuid"},
              "aggregate":    {"type": "string"},
              "aggregate_id": {"type": "string"},
              "topic":        {"type": "string"},
              "payload":      {"type": "object"},
              "headers":      {"type": "object", "additionalProperties": {"type": "string"}}
            }
          }' \
          "$BASE?ifExists=RETURN_OR_UPDATE" >/dev/null
        echo "Registering ontology.object.changed.v1 payload schema …"
        curl -fsS -X POST \
          -H 'Content-Type: application/json' \
          -H 'X-Registry-ArtifactId: ontology.object.changed.v1' \
          -H 'X-Registry-ArtifactType: JSON' \
          --data '{
            "$schema": "http://json-schema.org/draft-07/schema#",
            "title": "OntologyObjectChangedV1",
            "type": "object",
            "required": ["tenant", "type_id", "object_id", "version"],
            "properties": {
              "tenant":    {"type": "string"},
              "type_id":   {"type": "string"},
              "object_id": {"type": "string"},
              "version":   {"type": "integer", "minimum": 0}
            }
          }' \
          "$BASE?ifExists=RETURN_OR_UPDATE" >/dev/null
        echo "Done."

  # ── Iceberg REST Catalog ───────────────────────────────────
  # Apache Polaris was removed from the default DX stack on 2026-04-30
  # (see ADR-0008). In Kubernetes the Iceberg REST Catalog is provided by
  # Lakekeeper (`infra/k8s/lakekeeper/`); there is no Polaris/Lakekeeper
  # service in this Compose file. Services that read `ICEBERG_CATALOG_URL`
  # (e.g. dataset-versioning-service, event-streaming-service) treat the
  # variable as optional and fall back to the legacy dataset writer when
  # it is unset — see `services/*/storage/factory.rs`.

  identity-federation-service:
    <<: *app-common
    profiles: *profiles-foundation
    build:
      context: ..
      dockerfile: services/identity-federation-service/Dockerfile
    environment:
      OPENFOUNDRY_ENV: development
      HOST: 0.0.0.0
      PORT: 50112
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_auth_service
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
      PUBLIC_WEB_ORIGIN: ${OPENFOUNDRY_PUBLIC_WEB_ORIGIN:-http://localhost:3000}
      REDIS_URL: redis://valkey:6379
      NATS_URL: nats://nats:4222
    depends_on:
      postgres:
        condition: service_healthy
      valkey:
        condition: service_healthy
      nats:
        condition: service_healthy
    ports:
      - "${OPENFOUNDRY_IDENTITY_FEDERATION_HOST_PORT:-50112}:50112"

  cipher-service:
    <<: *app-common
    profiles: *profiles-foundation
    build:
      context: ..
      dockerfile: services/cipher-service/Dockerfile
    environment:
      OPENFOUNDRY_ENV: development
      HOST: 0.0.0.0
      PORT: 50073
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_auth_service
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "${OPENFOUNDRY_CIPHER_HOST_PORT:-50073}:50073"

  session-governance-service:
    <<: *app-common
    profiles: *profiles-foundation
    build:
      context: ..
      dockerfile: services/session-governance-service/Dockerfile
    environment:
      OPENFOUNDRY_ENV: development
      HOST: 0.0.0.0
      PORT: 50074
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_auth_service
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "${OPENFOUNDRY_SESSION_GOVERNANCE_HOST_PORT:-50074}:50074"

  authorization-policy-service:
    <<: *app-common
    profiles: *profiles-foundation
    build:
      context: ..
      dockerfile: services/authorization-policy-service/Dockerfile
    environment:
      OPENFOUNDRY_ENV: development
      HOST: 0.0.0.0
      PORT: 50093
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_auth_service
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "${OPENFOUNDRY_AUTHORIZATION_POLICY_HOST_PORT:-50093}:50093"

  security-governance-service:
    <<: *app-common
    profiles: *profiles-foundation
    build:
      context: ..
      dockerfile: services/security-governance-service/Dockerfile
    environment:
      OPENFOUNDRY_ENV: development
      HOST: 0.0.0.0
      PORT: 50114
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_audit_service
      AUDIT_DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_audit_service
      POLICY_DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_auth_service
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
    depends_on:
      postgres:
        condition: service_healthy
      authorization-policy-service:
        condition: service_started
    ports:
      - "${OPENFOUNDRY_SECURITY_GOVERNANCE_HOST_PORT:-50114}:50114"

  tenancy-organizations-service:
    <<: *app-common
    profiles: *profiles-foundation
    build:
      context: ..
      dockerfile: services/tenancy-organizations-service/Dockerfile
    environment:
      OPENFOUNDRY_ENV: development
      HOST: 0.0.0.0
      PORT: 50113
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_auth_service
      NEXUS_DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_nexus_service
      ONTOLOGY_DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_ontology_service
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
    depends_on:
      postgres:
        condition: service_healthy
      nexus-service:
        condition: service_started
    ports:
      - "${OPENFOUNDRY_TENANCY_ORGANIZATIONS_HOST_PORT:-50113}:50113"

  oauth-integration-service:
    <<: *app-common
    profiles: *profiles-foundation
    build:
      context: ..
      dockerfile: services/oauth-integration-service/Dockerfile
    environment:
      OPENFOUNDRY_ENV: development
      HOST: 0.0.0.0
      PORT: 50094
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_auth_service
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "${OPENFOUNDRY_OAUTH_INTEGRATION_HOST_PORT:-50094}:50094"

  network-boundary-service:
    <<: *app-common
    profiles: *profiles-foundation
    build:
      context: ..
      dockerfile: services/network-boundary-service/Dockerfile
    environment:
      OPENFOUNDRY_ENV: development
      HOST: 0.0.0.0
      PORT: 50119
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_auth_service
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "${OPENFOUNDRY_NETWORK_BOUNDARY_HOST_PORT:-50119}:50119"

  connector-management-service:
    <<: *app-common
    profiles: *profiles-data
    build:
      context: ..
      dockerfile: services/connector-management-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50088
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_data_connector
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
      DATASET_SERVICE_URL: http://data-asset-catalog-service:50079
      PIPELINE_SERVICE_URL: http://pipeline-authoring-service:50080
      ONTOLOGY_SERVICE_URL: http://ontology-definition-service:50103
      INGESTION_REPLICATION_SERVICE_URL: http://ingestion-replication-service:50090
      NETWORK_BOUNDARY_SERVICE_URL: http://network-boundary-service:50119
      ALLOW_PRIVATE_NETWORK_EGRESS: "true"
    depends_on:
      postgres:
        condition: service_healthy
      ontology-definition-service:
        condition: service_started
      ingestion-replication-service:
        condition: service_started
      network-boundary-service:
        condition: service_started
    ports:
      - "${OPENFOUNDRY_CONNECTOR_MANAGEMENT_HOST_PORT:-50088}:50088"

  virtual-table-service:
    <<: *app-common
    profiles: *profiles-data
    build:
      context: ..
      dockerfile: services/virtual-table-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50089
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_data_connector
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
      DATASET_SERVICE_URL: http://data-asset-catalog-service:50079
      PIPELINE_SERVICE_URL: http://pipeline-authoring-service:50080
      ONTOLOGY_SERVICE_URL: http://ontology-definition-service:50103
      ALLOW_PRIVATE_NETWORK_EGRESS: "true"
    depends_on:
      postgres:
        condition: service_healthy
      ontology-definition-service:
        condition: service_started
    ports:
      - "${OPENFOUNDRY_VIRTUAL_TABLE_HOST_PORT:-50089}:50089"

  ingestion-replication-service:
    <<: *app-common
    profiles: *profiles-data
    build:
      context: ..
      dockerfile: services/ingestion-replication-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50090
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_data_connector
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
      DATASET_SERVICE_URL: http://data-asset-catalog-service:50079
      PIPELINE_SERVICE_URL: http://pipeline-authoring-service:50080
      ONTOLOGY_SERVICE_URL: http://ontology-definition-service:50103
      ALLOW_PRIVATE_NETWORK_EGRESS: "true"
    depends_on:
      postgres:
        condition: service_healthy
      data-asset-catalog-service:
        condition: service_started
      ontology-definition-service:
        condition: service_started
    ports:
      - "${OPENFOUNDRY_INGESTION_REPLICATION_HOST_PORT:-50090}:50090"

  dataset-versioning-service:
    <<: *app-common
    profiles: *profiles-data
    build:
      context: ..
      dockerfile: services/dataset-versioning-service/Dockerfile
    environment:
      OPENFOUNDRY_ENV: development
      HOST: 0.0.0.0
      PORT: 50078
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_dataset_service
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "${OPENFOUNDRY_DATASET_VERSIONING_HOST_PORT:-50078}:50078"

  data-asset-catalog-service:
    <<: *app-common
    profiles: *profiles-data
    build:
      context: ..
      dockerfile: services/data-asset-catalog-service/Dockerfile
    environment:
      OPENFOUNDRY_ENV: development
      HOST: 0.0.0.0
      PORT: 50079
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_dataset_service
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
      STORAGE_BACKEND: s3
      STORAGE_BUCKET: datasets
      S3_ENDPOINT: http://minio:9000
      S3_REGION: us-east-1
      S3_ACCESS_KEY: minioadmin
      S3_SECRET_KEY: minioadmin
      DATASET_QUALITY_SERVICE_URL: http://dataset-quality-service:50072
    depends_on:
      postgres:
        condition: service_healthy
      minio:
        condition: service_healthy
      minio-init:
        condition: service_started
      dataset-quality-service:
        condition: service_started
    ports:
      - "${OPENFOUNDRY_DATASET_CATALOG_HOST_PORT:-50079}:50079"

  dataset-quality-service:
    <<: *app-common
    profiles: *profiles-data
    build:
      context: ..
      dockerfile: services/dataset-quality-service/Dockerfile
    environment:
      OPENFOUNDRY_ENV: development
      HOST: 0.0.0.0
      PORT: 50072
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_dataset_service
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
      STORAGE_BACKEND: s3
      STORAGE_BUCKET: datasets
      S3_ENDPOINT: http://minio:9000
      S3_REGION: us-east-1
      S3_ACCESS_KEY: minioadmin
      S3_SECRET_KEY: minioadmin
    depends_on:
      postgres:
        condition: service_healthy
      minio:
        condition: service_healthy
      minio-init:
        condition: service_started
    ports:
      - "${OPENFOUNDRY_DATASET_QUALITY_HOST_PORT:-50072}:50072"

  event-streaming-service:
    <<: *app-common
    profiles: *profiles-data
    build:
      context: ..
      dockerfile: services/event-streaming-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50121
      REST_PORT: 50121
      GRPC_PORT: 50221
      ADMIN_PORT: 50222
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_event_streaming_service
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
      ROUTES_FILE: /etc/openfoundry/topic-routes.yaml
      CONNECTOR_MANAGEMENT_SERVICE_URL: http://connector-management-service:50088
      DATASET_SERVICE_URL: http://data-asset-catalog-service:50079
      ICEBERG_CATALOG_URL: http://iceberg-rest:8181
      ICEBERG_NAMESPACE: streaming_service
      KAFKA_BOOTSTRAP_SERVERS: redpanda:9092
      ARCHIVE_DIR: /var/lib/openfoundry/event-streaming-archive
    depends_on:
      postgres:
        condition: service_healthy
      data-asset-catalog-service:
        condition: service_started
    volumes:
      - event_streaming_archive_data:/var/lib/openfoundry/event-streaming-archive
      - ../services/event-streaming-service/config/topic-routes.yaml:/etc/openfoundry/topic-routes.yaml:ro
    ports:
      - "${OPENFOUNDRY_EVENT_STREAMING_HOST_PORT:-50121}:50121"
      - "${OPENFOUNDRY_EVENT_STREAMING_GRPC_HOST_PORT:-50221}:50221"
      - "${OPENFOUNDRY_EVENT_STREAMING_ADMIN_HOST_PORT:-50222}:50222"

  cdc-metadata-service:
    <<: *app-common
    profiles: *profiles-data
    build:
      context: ..
      dockerfile: services/cdc-metadata-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50122
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_cdc_metadata_service
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
      DATA_CONNECTOR_URL: http://connector-management-service:50088
      EVENT_STREAMING_SERVICE_URL: http://event-streaming-service:50121
      PIPELINE_SERVICE_URL: http://pipeline-authoring-service:50080
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "${OPENFOUNDRY_CDC_METADATA_HOST_PORT:-50122}:50122"

  sql-warehousing-service:
    <<: *app-common
    profiles: *profiles-data
    build:
      context: ..
      dockerfile: services/sql-warehousing-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50123
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_sql_warehousing_service
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
      QUERY_SERVICE_URL: http://sql-bi-gateway-service:50133
      PIPELINE_SERVICE_URL: http://pipeline-authoring-service:50080
      DATASET_SERVICE_URL: http://data-asset-catalog-service:50079
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "${OPENFOUNDRY_SQL_WAREHOUSING_HOST_PORT:-50123}:50123"

  time-series-data-service:
    <<: *app-common
    profiles: *profiles-data
    build:
      context: ..
      dockerfile: services/time-series-data-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50124
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_time_series_data_service
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
      EVENT_STREAMING_SERVICE_URL: http://event-streaming-service:50121
      ONTOLOGY_SERVICE_URL: http://ontology-definition-service:50103
      GEOSPATIAL_SERVICE_URL: http://geospatial-intelligence-service:50068
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "${OPENFOUNDRY_TIME_SERIES_DATA_HOST_PORT:-50124}:50124"

  model-adapter-service:
    <<: *app-common
    profiles: *profiles-intelligence
    build:
      context: ..
      dockerfile: services/model-adapter-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50125
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_model_adapter_service
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
      MODEL_CATALOG_SERVICE_URL: http://model-catalog-service:50085
      MODEL_DEPLOYMENT_SERVICE_URL: http://model-deployment-service:50086
      MODEL_SERVING_SERVICE_URL: http://model-serving-service:50087
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "${OPENFOUNDRY_MODEL_ADAPTER_HOST_PORT:-50125}:50125"

  model-lifecycle-service:
    <<: *app-common
    profiles: *profiles-intelligence
    build:
      context: ..
      dockerfile: services/model-lifecycle-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50126
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_model_lifecycle_service
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
      MODEL_CATALOG_SERVICE_URL: http://model-catalog-service:50085
      MODEL_DEPLOYMENT_SERVICE_URL: http://model-deployment-service:50086
      APPROVALS_SERVICE_URL: http://approvals-service:50071
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "${OPENFOUNDRY_MODEL_LIFECYCLE_HOST_PORT:-50126}:50126"

  agent-runtime-service:
    <<: *app-common
    profiles: *profiles-intelligence
    build:
      context: ..
      dockerfile: services/agent-runtime-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50127
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_agent_runtime_service
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
      TOOL_REGISTRY_SERVICE_URL: http://tool-registry-service:50100
      CONVERSATION_STATE_SERVICE_URL: http://conversation-state-service:50099
      LLM_CATALOG_SERVICE_URL: http://llm-catalog-service:50095
      PROMPT_WORKFLOW_SERVICE_URL: http://prompt-workflow-service:50096
      RETRIEVAL_CONTEXT_SERVICE_URL: http://retrieval-context-service:50098
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "${OPENFOUNDRY_AGENT_RUNTIME_HOST_PORT:-50127}:50127"

  document-intelligence-service:
    <<: *app-common
    profiles: *profiles-intelligence
    build:
      context: ..
      dockerfile: services/document-intelligence-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50128
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_document_intelligence_service
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
      DATA_STORAGE_SERVICE_URL: http://data-asset-catalog-service:50079
      KNOWLEDGE_INDEX_SERVICE_URL: http://knowledge-index-service:50097
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "${OPENFOUNDRY_DOCUMENT_INTELLIGENCE_HOST_PORT:-50128}:50128"

  ai-application-generation-service:
    <<: *app-common
    profiles: *profiles-intelligence
    build:
      context: ..
      dockerfile: services/ai-application-generation-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50129
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_ai_application_generation_service
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
      APP_BUILDER_SERVICE_URL: http://app-builder-service:50063
      AGENT_RUNTIME_SERVICE_URL: http://agent-runtime-service:50127
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "${OPENFOUNDRY_AI_APPLICATION_GENERATION_HOST_PORT:-50129}:50129"

  tabular-analysis-service:
    <<: *app-common
    profiles: *profiles-intelligence
    build:
      context: ..
      dockerfile: services/tabular-analysis-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50130
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_tabular_analysis_service
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
      DATASET_SERVICE_URL: http://data-asset-catalog-service:50079
      QUERY_SERVICE_URL: http://sql-bi-gateway-service:50133
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "${OPENFOUNDRY_TABULAR_ANALYSIS_HOST_PORT:-50130}:50130"

  ontology-exploratory-analysis-service:
    <<: *app-common
    profiles: *profiles-intelligence
    build:
      context: ..
      dockerfile: services/ontology-exploratory-analysis-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50131
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_ontology_exploratory_analysis_service
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
      ONTOLOGY_QUERY_SERVICE_URL: http://ontology-query-service:50105
      ONTOLOGY_ACTIONS_SERVICE_URL: http://ontology-actions-service:50106
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "${OPENFOUNDRY_ONTOLOGY_EXPLORATORY_ANALYSIS_HOST_PORT:-50131}:50131"

  ontology-timeseries-analytics-service:
    <<: *app-common
    profiles: *profiles-intelligence
    build:
      context: ..
      dockerfile: services/ontology-timeseries-analytics-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50132
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_ontology_timeseries_analytics
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "${OPENFOUNDRY_ONTOLOGY_TIMESERIES_ANALYTICS_HOST_PORT:-50132}:50132"

  sql-bi-gateway-service:
    <<: *app-common
    profiles: *profiles-data
    build:
      context: ..
      dockerfile: services/sql-bi-gateway-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50133
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_query_service
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
      DATA_DIR: /var/lib/openfoundry/query-data
      DISTRIBUTED_QUERY_WORKERS: "1"
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - query_data:/var/lib/openfoundry/query-data
    ports:
      - "${OPENFOUNDRY_SQL_BI_GATEWAY_HOST_PORT:-50133}:50133"

  notebook-runtime-service:
    <<: *app-common
    profiles: *profiles-intelligence
    build:
      context: ..
      dockerfile: services/notebook-runtime-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50134
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_notebook_service
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
      DATA_DIR: /var/lib/openfoundry/notebook-data
      QUERY_SERVICE_URL: http://sql-bi-gateway-service:50133
      AI_SERVICE_URL: http://agent-runtime-service:50127
    depends_on:
      postgres:
        condition: service_healthy
      sql-bi-gateway-service:
        condition: service_started
      agent-runtime-service:
        condition: service_started
    volumes:
      - notebook_data:/var/lib/openfoundry/notebook-data
    ports:
      - "${OPENFOUNDRY_NOTEBOOK_RUNTIME_HOST_PORT:-50134}:50134"

  spreadsheet-computation-service:
    <<: *app-common
    profiles: *profiles-intelligence
    build:
      context: ..
      dockerfile: services/spreadsheet-computation-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50135
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_spreadsheet_computation
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "${OPENFOUNDRY_SPREADSHEET_COMPUTATION_HOST_PORT:-50135}:50135"

  analytical-logic-service:
    <<: *app-common
    profiles: *profiles-intelligence
    build:
      context: ..
      dockerfile: services/analytical-logic-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50136
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_analytical_logic
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "${OPENFOUNDRY_ANALYTICAL_LOGIC_HOST_PORT:-50136}:50136"

  workflow-automation-service:
    <<: *app-common
    profiles: *profiles-data
    build:
      context: ..
      dockerfile: services/workflow-automation-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50137
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_workflow_service
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
      APPROVALS_SERVICE_URL: http://approvals-service:50071
      NOTIFICATION_SERVICE_URL: http://notification-alerting-service:50114
      ONTOLOGY_SERVICE_URL: http://ontology-actions-service:50106
      PIPELINE_SERVICE_URL: http://lineage-service:50083
    depends_on:
      postgres:
        condition: service_healthy
      notification-alerting-service:
        condition: service_started
      lineage-service:
        condition: service_started
      ontology-actions-service:
        condition: service_started
    ports:
      - "${OPENFOUNDRY_WORKFLOW_AUTOMATION_HOST_PORT:-50137}:50137"

  automation-operations-service:
    <<: *app-common
    profiles: *profiles-experience
    build:
      context: ..
      dockerfile: services/automation-operations-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50138
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_automation_operations
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "${OPENFOUNDRY_AUTOMATION_OPERATIONS_HOST_PORT:-50138}:50138"

  workflow-trace-service:
    <<: *app-common
    profiles: *profiles-experience
    build:
      context: ..
      dockerfile: services/workflow-trace-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50139
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_workflow_trace
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "${OPENFOUNDRY_WORKFLOW_TRACE_HOST_PORT:-50139}:50139"

  application-composition-service:
    <<: *app-common
    profiles: *profiles-experience
    build:
      context: ..
      dockerfile: services/application-composition-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50140
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_application_composition
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "${OPENFOUNDRY_APPLICATION_COMPOSITION_HOST_PORT:-50140}:50140"

  scenario-simulation-service:
    <<: *app-common
    profiles: *profiles-experience
    build:
      context: ..
      dockerfile: services/scenario-simulation-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50141
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_scenario_simulation
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "${OPENFOUNDRY_SCENARIO_SIMULATION_HOST_PORT:-50141}:50141"

  solution-design-service:
    <<: *app-common
    profiles: *profiles-experience
    build:
      context: ..
      dockerfile: services/solution-design-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50142
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_solution_design
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "${OPENFOUNDRY_SOLUTION_DESIGN_HOST_PORT:-50142}:50142"

  developer-console-service:
    <<: *app-common
    profiles: *profiles-experience
    build:
      context: ..
      dockerfile: services/developer-console-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50143
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_developer_console
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "${OPENFOUNDRY_DEVELOPER_CONSOLE_HOST_PORT:-50143}:50143"

  sdk-generation-service:
    <<: *app-common
    profiles: *profiles-experience
    build:
      context: ..
      dockerfile: services/sdk-generation-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50144
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_sdk_generation
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "${OPENFOUNDRY_SDK_GENERATION_HOST_PORT:-50144}:50144"

  managed-workspace-service:
    <<: *app-common
    profiles: *profiles-experience
    build:
      context: ..
      dockerfile: services/managed-workspace-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50145
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_managed_workspace
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "${OPENFOUNDRY_MANAGED_WORKSPACE_HOST_PORT:-50145}:50145"

  custom-endpoints-service:
    <<: *app-common
    profiles: *profiles-experience
    build:
      context: ..
      dockerfile: services/custom-endpoints-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50146
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_custom_endpoints
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "${OPENFOUNDRY_CUSTOM_ENDPOINTS_HOST_PORT:-50146}:50146"

  mcp-orchestration-service:
    <<: *app-common
    profiles: *profiles-experience
    build:
      context: ..
      dockerfile: services/mcp-orchestration-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50147
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_mcp_orchestration
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "${OPENFOUNDRY_MCP_ORCHESTRATION_HOST_PORT:-50147}:50147"

  compute-modules-control-plane-service:
    <<: *app-common
    profiles: *profiles-experience
    build:
      context: ..
      dockerfile: services/compute-modules-control-plane-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50148
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_compute_modules_control_plane
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "${OPENFOUNDRY_COMPUTE_MODULES_CONTROL_PLANE_HOST_PORT:-50148}:50148"

  compute-modules-runtime-service:
    <<: *app-common
    profiles: *profiles-experience
    build:
      context: ..
      dockerfile: services/compute-modules-runtime-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50149
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_compute_modules_runtime
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "${OPENFOUNDRY_COMPUTE_MODULES_RUNTIME_HOST_PORT:-50149}:50149"

  monitoring-rules-service:
    <<: *app-common
    profiles: *profiles-experience
    build:
      context: ..
      dockerfile: services/monitoring-rules-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50150
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_monitoring_rules
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "${OPENFOUNDRY_MONITORING_RULES_HOST_PORT:-50150}:50150"

  health-check-service:
    <<: *app-common
    profiles: *profiles-experience
    build:
      context: ..
      dockerfile: services/health-check-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50151
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_health_check
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "${OPENFOUNDRY_HEALTH_CHECK_HOST_PORT:-50151}:50151"

  execution-observability-service:
    <<: *app-common
    profiles: *profiles-experience
    build:
      context: ..
      dockerfile: services/execution-observability-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50152
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_execution_observability
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "${OPENFOUNDRY_EXECUTION_OBSERVABILITY_HOST_PORT:-50152}:50152"

  telemetry-governance-service:
    <<: *app-common
    profiles: *profiles-experience
    build:
      context: ..
      dockerfile: services/telemetry-governance-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50153
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_telemetry_governance
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "${OPENFOUNDRY_TELEMETRY_GOVERNANCE_HOST_PORT:-50153}:50153"

  code-security-scanning-service:
    <<: *app-common
    profiles: *profiles-experience
    build:
      context: ..
      dockerfile: services/code-security-scanning-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50154
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_code_security_scanning
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "${OPENFOUNDRY_CODE_SECURITY_SCANNING_HOST_PORT:-50154}:50154"

  code-repository-review-service:
    <<: *app-common
    profiles: *profiles-experience
    build:
      context: ..
      dockerfile: services/code-repository-review-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50155
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_code_repository_review_service
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "${OPENFOUNDRY_CODE_REPOSITORY_REVIEW_HOST_PORT:-50155}:50155"


  pipeline-authoring-service:
    <<: *app-common
    profiles: *profiles-data
    build:
      context: ..
      dockerfile: services/pipeline-authoring-service/Dockerfile
    environment:
      OPENFOUNDRY_ENV: development
      HOST: 0.0.0.0
      PORT: 50080
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_pipeline_service
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
      DATA_DIR: /var/lib/openfoundry/pipeline-data
      DATASET_SERVICE_URL: http://data-asset-catalog-service:50079
      WORKFLOW_SERVICE_URL: http://workflow-automation-service:50137
      AI_SERVICE_URL: http://agent-runtime-service:50127
      STORAGE_BACKEND: s3
      STORAGE_BUCKET: datasets
      S3_ENDPOINT: http://minio:9000
      S3_REGION: us-east-1
      S3_ACCESS_KEY: minioadmin
      S3_SECRET_KEY: minioadmin
      DISTRIBUTED_PIPELINE_WORKERS: "1"
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - pipeline_data:/var/lib/openfoundry/pipeline-data
    ports:
      - "${OPENFOUNDRY_PIPELINE_AUTHORING_HOST_PORT:-50080}:50080"

  pipeline-build-service:
    <<: *app-common
    profiles: *profiles-data
    build:
      context: ..
      dockerfile: services/pipeline-build-service/Dockerfile
    environment:
      OPENFOUNDRY_ENV: development
      HOST: 0.0.0.0
      PORT: 50081
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_pipeline_service
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
      DATA_DIR: /var/lib/openfoundry/pipeline-data
      DATASET_SERVICE_URL: http://data-asset-catalog-service:50079
      WORKFLOW_SERVICE_URL: http://workflow-automation-service:50137
      AI_SERVICE_URL: http://agent-runtime-service:50127
      STORAGE_BACKEND: s3
      STORAGE_BUCKET: datasets
      S3_ENDPOINT: http://minio:9000
      S3_REGION: us-east-1
      S3_ACCESS_KEY: minioadmin
      S3_SECRET_KEY: minioadmin
      DISTRIBUTED_PIPELINE_WORKERS: "1"
    depends_on:
      postgres:
        condition: service_healthy
      minio:
        condition: service_healthy
      data-asset-catalog-service:
        condition: service_started
      workflow-automation-service:
        condition: service_started
    volumes:
      - pipeline_data:/var/lib/openfoundry/pipeline-data
    ports:
      - "${OPENFOUNDRY_PIPELINE_BUILD_HOST_PORT:-50081}:50081"

  pipeline-schedule-service:
    <<: *app-common
    profiles: *profiles-data
    build:
      context: ..
      dockerfile: services/pipeline-schedule-service/Dockerfile
    environment:
      OPENFOUNDRY_ENV: development
      HOST: 0.0.0.0
      PORT: 50082
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_pipeline_service
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
      DATA_DIR: /var/lib/openfoundry/pipeline-data
      DATASET_SERVICE_URL: http://data-asset-catalog-service:50079
      WORKFLOW_SERVICE_URL: http://workflow-automation-service:50137
      AI_SERVICE_URL: http://agent-runtime-service:50127
      STORAGE_BACKEND: s3
      STORAGE_BUCKET: datasets
      S3_ENDPOINT: http://minio:9000
      S3_REGION: us-east-1
      S3_ACCESS_KEY: minioadmin
      S3_SECRET_KEY: minioadmin
      DISTRIBUTED_PIPELINE_WORKERS: "1"
    depends_on:
      postgres:
        condition: service_healthy
      minio:
        condition: service_healthy
      data-asset-catalog-service:
        condition: service_started
      workflow-automation-service:
        condition: service_started
    volumes:
      - pipeline_data:/var/lib/openfoundry/pipeline-data
    ports:
      - "${OPENFOUNDRY_PIPELINE_SCHEDULE_HOST_PORT:-50082}:50082"

  lineage-service:
    <<: *app-common
    profiles: *profiles-data
    build:
      context: ..
      dockerfile: services/lineage-service/Dockerfile
    environment:
      OPENFOUNDRY_ENV: development
      HOST: 0.0.0.0
      PORT: 50083
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_pipeline_service
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
      QUERY_SERVICE_URL: http://sql-bi-gateway-service:50133
      DATASET_SERVICE_URL: http://data-asset-catalog-service:50079
      WORKFLOW_SERVICE_URL: http://workflow-automation-service:50137
      AI_SERVICE_URL: http://agent-runtime-service:50127
      STORAGE_BACKEND: s3
      STORAGE_BUCKET: datasets
      S3_ENDPOINT: http://minio:9000
      S3_REGION: us-east-1
      S3_ACCESS_KEY: minioadmin
      S3_SECRET_KEY: minioadmin
      DISTRIBUTED_PIPELINE_WORKERS: "1"
    depends_on:
      postgres:
        condition: service_healthy
      minio:
        condition: service_healthy
      data-asset-catalog-service:
        condition: service_started
    volumes:
      - pipeline_data:/var/lib/openfoundry/pipeline-data
    ports:
      - "${OPENFOUNDRY_LINEAGE_HOST_PORT:-50083}:50083"

  ontology-definition-service:
    <<: *app-common
    profiles: *profiles-data
    build:
      context: ..
      dockerfile: services/ontology-definition-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50103
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_ontology_service
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
      AUDIT_SERVICE_URL: http://audit-compliance-service:50115
      DATASET_SERVICE_URL: http://data-asset-catalog-service:50079
      ONTOLOGY_SERVICE_URL: http://gateway:8080
      PIPELINE_SERVICE_URL: http://pipeline-build-service:50081
      AI_SERVICE_URL: http://agent-runtime-service:50127
      NOTIFICATION_SERVICE_URL: http://notification-alerting-service:50114
      NODE_RUNTIME_COMMAND: node
      SEARCH_EMBEDDING_PROVIDER: deterministic-hash
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "${OPENFOUNDRY_ONTOLOGY_DEFINITION_HOST_PORT:-50103}:50103"

  object-database-service:
    <<: *app-common
    profiles: *profiles-knowledge
    build:
      context: ..
      dockerfile: services/object-database-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50104
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_ontology_service
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
      AUDIT_SERVICE_URL: http://audit-compliance-service:50115
      DATASET_SERVICE_URL: http://data-asset-catalog-service:50079
      ONTOLOGY_SERVICE_URL: http://gateway:8080
      PIPELINE_SERVICE_URL: http://pipeline-build-service:50081
      AI_SERVICE_URL: http://agent-runtime-service:50127
      NOTIFICATION_SERVICE_URL: http://notification-alerting-service:50114
      NODE_RUNTIME_COMMAND: node
      SEARCH_EMBEDDING_PROVIDER: deterministic-hash
    depends_on:
      postgres:
        condition: service_healthy
      notification-alerting-service:
        condition: service_started
    ports:
      - "${OPENFOUNDRY_OBJECT_DATABASE_HOST_PORT:-50104}:50104"

  ontology-query-service:
    <<: *app-common
    profiles: *profiles-knowledge
    build:
      context: ..
      dockerfile: services/ontology-query-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50105
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_ontology_service
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
      AUDIT_SERVICE_URL: http://audit-compliance-service:50115
      DATASET_SERVICE_URL: http://data-asset-catalog-service:50079
      ONTOLOGY_SERVICE_URL: http://gateway:8080
      PIPELINE_SERVICE_URL: http://pipeline-build-service:50081
      AI_SERVICE_URL: http://agent-runtime-service:50127
      NOTIFICATION_SERVICE_URL: http://notification-alerting-service:50114
      NODE_RUNTIME_COMMAND: node
      SEARCH_EMBEDDING_PROVIDER: deterministic-hash
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "${OPENFOUNDRY_ONTOLOGY_QUERY_HOST_PORT:-50105}:50105"

  ontology-actions-service:
    <<: *app-common
    profiles: *profiles-data
    build:
      context: ..
      dockerfile: services/ontology-actions-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50106
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_ontology_service
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
      AUDIT_SERVICE_URL: http://audit-compliance-service:50115
      DATASET_SERVICE_URL: http://data-asset-catalog-service:50079
      ONTOLOGY_SERVICE_URL: http://gateway:8080
      PIPELINE_SERVICE_URL: http://pipeline-build-service:50081
      AI_SERVICE_URL: http://agent-runtime-service:50127
      NOTIFICATION_SERVICE_URL: http://notification-alerting-service:50114
      NODE_RUNTIME_COMMAND: node
      SEARCH_EMBEDDING_PROVIDER: deterministic-hash
    depends_on:
      postgres:
        condition: service_healthy
      notification-alerting-service:
        condition: service_started
    ports:
      - "${OPENFOUNDRY_ONTOLOGY_ACTIONS_HOST_PORT:-50106}:50106"

  ontology-funnel-service:
    <<: *app-common
    profiles: *profiles-knowledge
    build:
      context: ..
      dockerfile: services/ontology-funnel-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50107
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_ontology_service
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
      AUDIT_SERVICE_URL: http://audit-compliance-service:50115
      DATASET_SERVICE_URL: http://data-asset-catalog-service:50079
      ONTOLOGY_SERVICE_URL: http://gateway:8080
      PIPELINE_SERVICE_URL: http://pipeline-build-service:50081
      AI_SERVICE_URL: http://agent-runtime-service:50127
      NOTIFICATION_SERVICE_URL: http://notification-alerting-service:50114
      NODE_RUNTIME_COMMAND: node
      SEARCH_EMBEDDING_PROVIDER: deterministic-hash
    depends_on:
      postgres:
        condition: service_healthy
      data-asset-catalog-service:
        condition: service_started
      pipeline-build-service:
        condition: service_started
    ports:
      - "${OPENFOUNDRY_ONTOLOGY_FUNNEL_HOST_PORT:-50107}:50107"

  ontology-functions-service:
    <<: *app-common
    profiles: *profiles-knowledge
    build:
      context: ..
      dockerfile: services/ontology-functions-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50108
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_ontology_service
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
      AUDIT_SERVICE_URL: http://audit-compliance-service:50115
      DATASET_SERVICE_URL: http://data-asset-catalog-service:50079
      ONTOLOGY_SERVICE_URL: http://gateway:8080
      PIPELINE_SERVICE_URL: http://pipeline-build-service:50081
      AI_SERVICE_URL: http://agent-runtime-service:50127
      NOTIFICATION_SERVICE_URL: http://notification-alerting-service:50114
      NODE_RUNTIME_COMMAND: node
      SEARCH_EMBEDDING_PROVIDER: deterministic-hash
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "${OPENFOUNDRY_ONTOLOGY_FUNCTIONS_HOST_PORT:-50108}:50108"

  ontology-security-service:
    <<: *app-common
    profiles: *profiles-knowledge
    build:
      context: ..
      dockerfile: services/ontology-security-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50109
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_ontology_service
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
      AUDIT_SERVICE_URL: http://audit-compliance-service:50115
      DATASET_SERVICE_URL: http://data-asset-catalog-service:50079
      ONTOLOGY_SERVICE_URL: http://gateway:8080
      PIPELINE_SERVICE_URL: http://pipeline-build-service:50081
      AI_SERVICE_URL: http://agent-runtime-service:50127
      NOTIFICATION_SERVICE_URL: http://notification-alerting-service:50114
      NODE_RUNTIME_COMMAND: node
      SEARCH_EMBEDDING_PROVIDER: deterministic-hash
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "${OPENFOUNDRY_ONTOLOGY_SECURITY_HOST_PORT:-50109}:50109"

  entity-resolution-service:
    <<: *app-common
    profiles: *profiles-knowledge
    build:
      context: ..
      dockerfile: services/entity-resolution-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50058
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_fusion_service
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "${OPENFOUNDRY_ENTITY_RESOLUTION_HOST_PORT:-50058}:50058"

  ml-experiments-service:
    <<: *app-common
    profiles: *profiles-intelligence
    build:
      context: ..
      dockerfile: services/ml-experiments-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50084
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_ml_service
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
      OPENFOUNDRY_ENV: development
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "${OPENFOUNDRY_ML_EXPERIMENTS_HOST_PORT:-50084}:50084"

  model-catalog-service:
    <<: *app-common
    profiles: *profiles-intelligence
    build:
      context: ..
      dockerfile: services/model-catalog-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50085
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_ml_service
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
      OPENFOUNDRY_ENV: development
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "${OPENFOUNDRY_MODEL_CATALOG_HOST_PORT:-50085}:50085"

  model-deployment-service:
    <<: *app-common
    profiles: *profiles-intelligence
    build:
      context: ..
      dockerfile: services/model-deployment-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50086
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_ml_service
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
      OPENFOUNDRY_ENV: development
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "${OPENFOUNDRY_MODEL_DEPLOYMENT_HOST_PORT:-50086}:50086"

  model-evaluation-service:
    <<: *app-common
    profiles: *profiles-intelligence
    build:
      context: ..
      dockerfile: services/model-evaluation-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50091
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_ml_service
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
      OPENFOUNDRY_ENV: development
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "${OPENFOUNDRY_MODEL_EVALUATION_HOST_PORT:-50091}:50091"

  model-serving-service:
    <<: *app-common
    profiles: *profiles-intelligence
    build:
      context: ..
      dockerfile: services/model-serving-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50087
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_ml_service
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
      OPENFOUNDRY_ENV: development
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "${OPENFOUNDRY_MODEL_SERVING_HOST_PORT:-50087}:50087"

  model-inference-history-service:
    <<: *app-common
    profiles: *profiles-intelligence
    build:
      context: ..
      dockerfile: services/model-inference-history-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50092
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_ml_service
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
      OPENFOUNDRY_ENV: development
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "${OPENFOUNDRY_MODEL_INFERENCE_HISTORY_HOST_PORT:-50092}:50092"

  ai-evaluation-service:
    <<: *app-common
    profiles: *profiles-intelligence
    build:
      context: ..
      dockerfile: services/ai-evaluation-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50075
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_ai_service
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "${OPENFOUNDRY_AI_EVALUATION_HOST_PORT:-50075}:50075"

  llm-catalog-service:
    <<: *app-common
    profiles: *profiles-intelligence
    build:
      context: ..
      dockerfile: services/llm-catalog-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50095
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_ai_service
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "${OPENFOUNDRY_LLM_CATALOG_HOST_PORT:-50095}:50095"

  prompt-workflow-service:
    <<: *app-common
    profiles: *profiles-intelligence
    build:
      context: ..
      dockerfile: services/prompt-workflow-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50096
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_ai_service
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "${OPENFOUNDRY_PROMPT_WORKFLOW_HOST_PORT:-50096}:50096"

  knowledge-index-service:
    <<: *app-common
    profiles: *profiles-intelligence
    build:
      context: ..
      dockerfile: services/knowledge-index-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50097
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_ai_service
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "${OPENFOUNDRY_KNOWLEDGE_INDEX_HOST_PORT:-50097}:50097"

  retrieval-context-service:
    <<: *app-common
    profiles: *profiles-intelligence
    build:
      context: ..
      dockerfile: services/retrieval-context-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50098
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_ai_service
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "${OPENFOUNDRY_RETRIEVAL_CONTEXT_HOST_PORT:-50098}:50098"

  conversation-state-service:
    <<: *app-common
    profiles: *profiles-intelligence
    build:
      context: ..
      dockerfile: services/conversation-state-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50099
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_ai_service
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "${OPENFOUNDRY_CONVERSATION_STATE_HOST_PORT:-50099}:50099"

  tool-registry-service:
    <<: *app-common
    profiles: *profiles-intelligence
    build:
      context: ..
      dockerfile: services/tool-registry-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50100
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_ai_service
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "${OPENFOUNDRY_TOOL_REGISTRY_HOST_PORT:-50100}:50100"

  approvals-service:
    <<: *app-common
    profiles: *profiles-experience
    build:
      context: ..
      dockerfile: services/approvals-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50071
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_workflow_service
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
      WORKFLOW_SERVICE_URL: http://workflow-automation-service:50137
      ONTOLOGY_SERVICE_URL: http://ontology-actions-service:50106
    depends_on:
      postgres:
        condition: service_healthy
      workflow-automation-service:
        condition: service_started
      ontology-actions-service:
        condition: service_started
    ports:
      - "${OPENFOUNDRY_APPROVALS_HOST_PORT:-50071}:50071"

  document-reporting-service:
    <<: *app-common
    profiles: *profiles-experience
    build:
      context: ..
      dockerfile: services/document-reporting-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50102
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_notebook_service
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "${OPENFOUNDRY_DOCUMENT_REPORTING_HOST_PORT:-50102}:50102"

  app-builder-service:
    <<: *app-common
    profiles: *profiles-experience
    build:
      context: ..
      dockerfile: services/app-builder-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50063
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_app_builder_service
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
      PUBLIC_BASE_URL: ${OPENFOUNDRY_PUBLIC_BASE_URL:-http://localhost:3000/apps/runtime}
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "${OPENFOUNDRY_APP_BUILDER_HOST_PORT:-50063}:50063"

  application-curation-service:
    <<: *app-common
    profiles: *profiles-experience
    build:
      context: ..
      dockerfile: services/application-curation-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50101
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_app_builder_service
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
      PUBLIC_BASE_URL: ${OPENFOUNDRY_PUBLIC_BASE_URL:-http://localhost:3000/apps/runtime}
    depends_on:
      postgres:
        condition: service_healthy
      app-builder-service:
        condition: service_started
    ports:
      - "${OPENFOUNDRY_APPLICATION_CURATION_HOST_PORT:-50101}:50101"

  widget-registry-service:
    <<: *app-common
    profiles: *profiles-experience
    build:
      context: ..
      dockerfile: services/widget-registry-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50077
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
      OPENFOUNDRY_ENV: development
    ports:
      - "${OPENFOUNDRY_WIDGET_REGISTRY_HOST_PORT:-50077}:50077"

  report-service:
    <<: *app-common
    profiles: *profiles-knowledge
    build:
      context: ..
      dockerfile: services/report-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50064
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_report_service
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
      DATASET_SERVICE_URL: http://data-asset-catalog-service:50079
      GEOSPATIAL_SERVICE_URL: http://geospatial-intelligence-service:50068
      OBJECT_STORAGE_BACKEND: local
      LOCAL_DELIVERY_ROOT: /var/lib/openfoundry/report-delivery
      REPORT_DELIVERY_TIMEOUT_SECS: "10"
      REPORT_DELIVERY_MAX_RETRIES: "2"
    depends_on:
      postgres:
        condition: service_healthy
      data-asset-catalog-service:
        condition: service_started
      geospatial-intelligence-service:
        condition: service_started
    volumes:
      - report_delivery_data:/var/lib/openfoundry/report-delivery
    ports:
      - "${OPENFOUNDRY_REPORT_HOST_PORT:-50064}:50064"

  global-branch-service:
    <<: *app-common
    profiles: *profiles-experience
    build:
      context: ..
      dockerfile: services/global-branch-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50110
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_code_repo_service
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
      REPO_STORAGE_ROOT: /var/lib/openfoundry/code-repos
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - code_repo_data:/var/lib/openfoundry/code-repos
    ports:
      - "${OPENFOUNDRY_GLOBAL_BRANCH_HOST_PORT:-50110}:50110"

  marketplace-service:
    <<: *app-common
    profiles: *profiles-experience
    build:
      context: ..
      dockerfile: services/marketplace-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50066
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_marketplace_service
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
      APP_BUILDER_SERVICE_URL: http://app-builder-service:50063
      DEVOPS_RECONCILER_INTERVAL_SECONDS: "300"
    depends_on:
      postgres:
        condition: service_healthy
      app-builder-service:
        condition: service_started

  marketplace-catalog-service:
    <<: *app-common
    profiles: *profiles-experience
    build:
      context: ..
      dockerfile: services/marketplace-catalog-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50066
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_marketplace_service
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
      APP_BUILDER_SERVICE_URL: http://app-builder-service:50063
    depends_on:
      postgres:
        condition: service_healthy
      marketplace-service:
        condition: service_started
      app-builder-service:
        condition: service_started
    ports:
      - "${OPENFOUNDRY_MARKETPLACE_CATALOG_HOST_PORT:-50066}:50066"

  product-distribution-service:
    <<: *app-common
    profiles: *profiles-experience
    build:
      context: ..
      dockerfile: services/product-distribution-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50111
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_marketplace_service
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
      APP_BUILDER_SERVICE_URL: http://app-builder-service:50063
      DEVOPS_RECONCILER_INTERVAL_SECONDS: "300"
    depends_on:
      postgres:
        condition: service_healthy
      marketplace-service:
        condition: service_started
      app-builder-service:
        condition: service_started
    ports:
      - "${OPENFOUNDRY_PRODUCT_DISTRIBUTION_HOST_PORT:-50111}:50111"

  federation-product-exchange-service:
    <<: *app-common
    profiles: *profiles-experience
    build:
      context: ..
      dockerfile: services/federation-product-exchange-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50120
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_nexus_service
      MARKETPLACE_DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_marketplace_service
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
    depends_on:
      postgres:
        condition: service_healthy
      nexus-service:
        condition: service_started
      marketplace-catalog-service:
        condition: service_started
      product-distribution-service:
        condition: service_started
    ports:
      - "${OPENFOUNDRY_FEDERATION_PRODUCT_EXCHANGE_HOST_PORT:-50120}:50120"

  nexus-service:
    <<: *app-common
    profiles: *profiles-foundation
    build:
      context: ..
      dockerfile: services/nexus-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50067
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_nexus_service
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "${OPENFOUNDRY_NEXUS_HOST_PORT:-50067}:50067"

  geospatial-intelligence-service:
    <<: *app-common
    profiles: *profiles-knowledge
    build:
      context: ..
      dockerfile: services/geospatial-intelligence-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50068
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_geospatial_service
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "${OPENFOUNDRY_GEOSPATIAL_INTELLIGENCE_HOST_PORT:-50068}:50068"

  notification-alerting-service:
    <<: *app-common
    profiles: *profiles-foundation
    build:
      context: ..
      dockerfile: services/notification-alerting-service/Dockerfile
    environment:
      OPENFOUNDRY_ENV: development
      HOST: 0.0.0.0
      PORT: 50114
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_notification_service
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
      NATS_URL: nats://nats:4222
    depends_on:
      postgres:
        condition: service_healthy
      nats:
        condition: service_healthy
    ports:
      - "${OPENFOUNDRY_NOTIFICATION_HOST_PORT:-50114}:50114"

  audit-compliance-service:
    <<: *app-common
    profiles: *profiles-foundation
    build:
      context: ..
      dockerfile: services/audit-compliance-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50115
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_audit_service
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
      NATS_URL: nats://nats:4222
    depends_on:
      postgres:
        condition: service_healthy
      nats:
        condition: service_healthy
    ports:
      - "${OPENFOUNDRY_AUDIT_COMPLIANCE_HOST_PORT:-50115}:50115"

  checkpoints-purpose-service:
    <<: *app-common
    profiles: *profiles-foundation
    build:
      context: ..
      dockerfile: services/checkpoints-purpose-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50116
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_audit_service
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "${OPENFOUNDRY_CHECKPOINTS_PURPOSE_HOST_PORT:-50116}:50116"

  retention-policy-service:
    <<: *app-common
    profiles: *profiles-data
    build:
      context: ..
      dockerfile: services/retention-policy-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50117
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_dataset_service
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "${OPENFOUNDRY_RETENTION_POLICY_HOST_PORT:-50117}:50117"

  lineage-deletion-service:
    <<: *app-common
    profiles: *profiles-data
    build:
      context: ..
      dockerfile: services/lineage-deletion-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50118
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_lineage_service
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
      LINEAGE_SERVICE_URL: http://lineage-service:50083
      DATASET_SERVICE_URL: http://data-asset-catalog-service:50079
      AUDIT_COMPLIANCE_SERVICE_URL: http://audit-compliance-service:50115
      STORAGE_BACKEND: ${OPENFOUNDRY_STORAGE_BACKEND:-local}
      STORAGE_BUCKET: ${OPENFOUNDRY_STORAGE_BUCKET:-datasets}
      LOCAL_STORAGE_ROOT: ${OPENFOUNDRY_LOCAL_STORAGE_ROOT:-/tmp/of-datasets}
    depends_on:
      postgres:
        condition: service_healthy
      lineage-service:
        condition: service_started
      data-asset-catalog-service:
        condition: service_started
      audit-compliance-service:
        condition: service_started
    ports:
      - "${OPENFOUNDRY_LINEAGE_DELETION_HOST_PORT:-50118}:50118"

  sds-service:
    <<: *app-common
    profiles: *profiles-foundation
    build:
      context: ..
      dockerfile: services/sds-service/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 50076
      DATABASE_URL: postgres://openfoundry:openfoundry@postgres:5432/openfoundry_audit_service
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
      OPENFOUNDRY_ENV: development
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "${OPENFOUNDRY_SDS_HOST_PORT:-50076}:50076"

  gateway:
    <<: *app-common
    profiles: *profiles-edge
    build:
      context: ..
      dockerfile: services/edge-gateway-service/Dockerfile
    environment:
      OPENFOUNDRY_ENV: development
      HOST: 0.0.0.0
      PORT: 8080
      JWT_SECRET: ${OPENFOUNDRY_JWT_SECRET:-openfoundry-dev-secret}
      REDIS_URL: redis://valkey:6379
      AUTH_SERVICE_URL: http://identity-federation-service:50112
      IDENTITY_FEDERATION_SERVICE_URL: http://identity-federation-service:50112
      SESSION_GOVERNANCE_SERVICE_URL: http://session-governance-service:50074
      AUTHORIZATION_POLICY_SERVICE_URL: http://authorization-policy-service:50093
      SECURITY_GOVERNANCE_SERVICE_URL: http://security-governance-service:50114
      TENANCY_ORGANIZATIONS_SERVICE_URL: http://tenancy-organizations-service:50113
      OAUTH_INTEGRATION_SERVICE_URL: http://oauth-integration-service:50094
      CIPHER_SERVICE_URL: http://cipher-service:50073
      DATA_CONNECTOR_SERVICE_URL: http://connector-management-service:50088
      CONNECTOR_MANAGEMENT_SERVICE_URL: http://connector-management-service:50088
      VIRTUAL_TABLE_SERVICE_URL: http://virtual-table-service:50089
      INGESTION_REPLICATION_SERVICE_URL: http://ingestion-replication-service:50090
      DATASET_VERSIONING_SERVICE_URL: http://dataset-versioning-service:50078
      DATA_ASSET_CATALOG_SERVICE_URL: http://data-asset-catalog-service:50079
      DATASET_QUALITY_SERVICE_URL: http://dataset-quality-service:50072
      EVENT_STREAMING_SERVICE_URL: http://event-streaming-service:50121
      CDC_METADATA_SERVICE_URL: http://cdc-metadata-service:50122
      SQL_WAREHOUSING_SERVICE_URL: http://sql-warehousing-service:50123
      TIME_SERIES_DATA_SERVICE_URL: http://time-series-data-service:50124
      QUERY_SERVICE_URL: http://sql-bi-gateway-service:50133
      PIPELINE_AUTHORING_SERVICE_URL: http://pipeline-authoring-service:50080
      PIPELINE_BUILD_SERVICE_URL: http://pipeline-build-service:50081
      PIPELINE_SCHEDULE_SERVICE_URL: http://pipeline-schedule-service:50082
      LINEAGE_SERVICE_URL: http://lineage-service:50083
      ONTOLOGY_DEFINITION_SERVICE_URL: http://ontology-definition-service:50103
      OBJECT_DATABASE_SERVICE_URL: http://object-database-service:50104
      ONTOLOGY_QUERY_SERVICE_URL: http://ontology-query-service:50105
      ONTOLOGY_ACTIONS_SERVICE_URL: http://ontology-actions-service:50106
      ONTOLOGY_FUNNEL_SERVICE_URL: http://ontology-funnel-service:50107
      ONTOLOGY_FUNCTIONS_SERVICE_URL: http://ontology-functions-service:50108
      ONTOLOGY_SECURITY_SERVICE_URL: http://ontology-security-service:50109
      ONTOLOGY_SERVICE_URL: http://ontology-definition-service:50103
      ENTITY_RESOLUTION_SERVICE_URL: http://entity-resolution-service:50058
      ML_SERVICE_URL: http://model-catalog-service:50085
      ML_EXPERIMENTS_SERVICE_URL: http://ml-experiments-service:50084
      MODEL_CATALOG_SERVICE_URL: http://model-catalog-service:50085
      MODEL_DEPLOYMENT_SERVICE_URL: http://model-deployment-service:50086
      MODEL_EVALUATION_SERVICE_URL: http://model-evaluation-service:50091
      MODEL_SERVING_SERVICE_URL: http://model-serving-service:50087
      MODEL_INFERENCE_HISTORY_SERVICE_URL: http://model-inference-history-service:50092
      AI_SERVICE_URL: http://agent-runtime-service:50127
      CHECKPOINTS_PURPOSE_SERVICE_URL: http://checkpoints-purpose-service:50116
      NETWORK_BOUNDARY_SERVICE_URL: http://network-boundary-service:50119
      RETENTION_POLICY_SERVICE_URL: http://retention-policy-service:50117
      LINEAGE_DELETION_SERVICE_URL: http://lineage-deletion-service:50118
      LLM_CATALOG_SERVICE_URL: http://llm-catalog-service:50095
      PROMPT_WORKFLOW_SERVICE_URL: http://prompt-workflow-service:50096
      KNOWLEDGE_INDEX_SERVICE_URL: http://knowledge-index-service:50097
      RETRIEVAL_CONTEXT_SERVICE_URL: http://retrieval-context-service:50098
      CONVERSATION_STATE_SERVICE_URL: http://conversation-state-service:50099
      TOOL_REGISTRY_SERVICE_URL: http://tool-registry-service:50100
      AI_EVALUATION_SERVICE_URL: http://ai-evaluation-service:50075
      WORKFLOW_SERVICE_URL: http://workflow-automation-service:50137
      APPROVALS_SERVICE_URL: http://approvals-service:50071
      NOTEBOOK_SERVICE_URL: http://notebook-runtime-service:50134
      DOCUMENT_REPORTING_SERVICE_URL: http://document-reporting-service:50102
      APP_BUILDER_SERVICE_URL: http://app-builder-service:50063
      APPLICATION_CURATION_SERVICE_URL: http://application-curation-service:50101
      WIDGET_REGISTRY_SERVICE_URL: http://widget-registry-service:50077
      REPORT_SERVICE_URL: http://report-service:50064
      GLOBAL_BRANCH_SERVICE_URL: http://global-branch-service:50110
      MARKETPLACE_CATALOG_SERVICE_URL: http://marketplace-catalog-service:50066
      PRODUCT_DISTRIBUTION_SERVICE_URL: http://product-distribution-service:50111
      FEDERATION_PRODUCT_EXCHANGE_SERVICE_URL: http://federation-product-exchange-service:50120
      NEXUS_SERVICE_URL: http://nexus-service:50067
      GEOSPATIAL_INTELLIGENCE_SERVICE_URL: http://geospatial-intelligence-service:50068
      NOTIFICATION_SERVICE_URL: http://notification-alerting-service:50114
      AUDIT_COMPLIANCE_SERVICE_URL: http://audit-compliance-service:50115
      AUDIT_SERVICE_URL: http://audit-compliance-service:50115
      SDS_SERVICE_URL: http://sds-service:50076
      MODEL_ADAPTER_SERVICE_URL: http://model-adapter-service:50125
      MODEL_LIFECYCLE_SERVICE_URL: http://model-lifecycle-service:50126
      AGENT_RUNTIME_SERVICE_URL: http://agent-runtime-service:50127
      DOCUMENT_INTELLIGENCE_SERVICE_URL: http://document-intelligence-service:50128
      AI_APPLICATION_GENERATION_SERVICE_URL: http://ai-application-generation-service:50129
      TABULAR_ANALYSIS_SERVICE_URL: http://tabular-analysis-service:50130
      ONTOLOGY_EXPLORATORY_ANALYSIS_SERVICE_URL: http://ontology-exploratory-analysis-service:50131
      ONTOLOGY_TIMESERIES_ANALYTICS_SERVICE_URL: http://ontology-timeseries-analytics-service:50132
      SQL_BI_GATEWAY_SERVICE_URL: http://sql-bi-gateway-service:50133
      NOTEBOOK_RUNTIME_SERVICE_URL: http://notebook-runtime-service:50134
      SPREADSHEET_COMPUTATION_SERVICE_URL: http://spreadsheet-computation-service:50135
      ANALYTICAL_LOGIC_SERVICE_URL: http://analytical-logic-service:50136
      WORKFLOW_AUTOMATION_SERVICE_URL: http://workflow-automation-service:50137
      AUTOMATION_OPERATIONS_SERVICE_URL: http://automation-operations-service:50138
      WORKFLOW_TRACE_SERVICE_URL: http://workflow-trace-service:50139
      APPLICATION_COMPOSITION_SERVICE_URL: http://application-composition-service:50140
      SCENARIO_SIMULATION_SERVICE_URL: http://scenario-simulation-service:50141
      SOLUTION_DESIGN_SERVICE_URL: http://solution-design-service:50142
      DEVELOPER_CONSOLE_SERVICE_URL: http://developer-console-service:50143
      SDK_GENERATION_SERVICE_URL: http://sdk-generation-service:50144
      MANAGED_WORKSPACE_SERVICE_URL: http://managed-workspace-service:50145
      CUSTOM_ENDPOINTS_SERVICE_URL: http://custom-endpoints-service:50146
      MCP_ORCHESTRATION_SERVICE_URL: http://mcp-orchestration-service:50147
      COMPUTE_MODULES_CONTROL_PLANE_SERVICE_URL: http://compute-modules-control-plane-service:50148
      COMPUTE_MODULES_RUNTIME_SERVICE_URL: http://compute-modules-runtime-service:50149
      MONITORING_RULES_SERVICE_URL: http://monitoring-rules-service:50150
      HEALTH_CHECK_SERVICE_URL: http://health-check-service:50151
      EXECUTION_OBSERVABILITY_SERVICE_URL: http://execution-observability-service:50152
      TELEMETRY_GOVERNANCE_SERVICE_URL: http://telemetry-governance-service:50153
      CODE_SECURITY_SCANNING_SERVICE_URL: http://code-security-scanning-service:50154
      CODE_REPOSITORY_REVIEW_SERVICE_URL: http://code-repository-review-service:50155
    depends_on:
      valkey:
        condition: service_healthy
      checkpoints-purpose-service:
        condition: service_started
      network-boundary-service:
        condition: service_started
      retention-policy-service:
        condition: service_started
      lineage-deletion-service:
        condition: service_started
      identity-federation-service:
        condition: service_started
      cipher-service:
        condition: service_started
      session-governance-service:
        condition: service_started
      authorization-policy-service:
        condition: service_started
      security-governance-service:
        condition: service_started
      tenancy-organizations-service:
        condition: service_started
      oauth-integration-service:
        condition: service_started
      connector-management-service:
        condition: service_started
      virtual-table-service:
        condition: service_started
      ingestion-replication-service:
        condition: service_started
      dataset-versioning-service:
        condition: service_started
      data-asset-catalog-service:
        condition: service_started
      dataset-quality-service:
        condition: service_started
      event-streaming-service:
        condition: service_started
      cdc-metadata-service:
        condition: service_started
      sql-warehousing-service:
        condition: service_started
      time-series-data-service:
        condition: service_started
      sql-bi-gateway-service:
        condition: service_started
      pipeline-authoring-service:
        condition: service_started
      pipeline-build-service:
        condition: service_started
      pipeline-schedule-service:
        condition: service_started
      lineage-service:
        condition: service_started
      ontology-definition-service:
        condition: service_started
      object-database-service:
        condition: service_started
      ontology-query-service:
        condition: service_started
      ontology-actions-service:
        condition: service_started
      ontology-funnel-service:
        condition: service_started
      ontology-functions-service:
        condition: service_started
      ontology-security-service:
        condition: service_started
      entity-resolution-service:
        condition: service_started
      ml-experiments-service:
        condition: service_started
      model-catalog-service:
        condition: service_started
      model-evaluation-service:
        condition: service_started
      model-inference-history-service:
        condition: service_started
      model-deployment-service:
        condition: service_started
      model-serving-service:
        condition: service_started
      llm-catalog-service:
        condition: service_started
      prompt-workflow-service:
        condition: service_started
      knowledge-index-service:
        condition: service_started
      retrieval-context-service:
        condition: service_started
      conversation-state-service:
        condition: service_started
      tool-registry-service:
        condition: service_started
      ai-evaluation-service:
        condition: service_started
      workflow-automation-service:
        condition: service_started
      approvals-service:
        condition: service_started
      notebook-runtime-service:
        condition: service_started
      document-reporting-service:
        condition: service_started
      app-builder-service:
        condition: service_started
      application-curation-service:
        condition: service_started
      widget-registry-service:
        condition: service_started
      report-service:
        condition: service_started
      global-branch-service:
        condition: service_started
      marketplace-catalog-service:
        condition: service_started
      product-distribution-service:
        condition: service_started
      federation-product-exchange-service:
        condition: service_started
      nexus-service:
        condition: service_started
      geospatial-intelligence-service:
        condition: service_started
      notification-alerting-service:
        condition: service_started
      audit-compliance-service:
        condition: service_started
      sds-service:
        condition: service_started
      model-adapter-service:
        condition: service_started
      model-lifecycle-service:
        condition: service_started
      agent-runtime-service:
        condition: service_started
      document-intelligence-service:
        condition: service_started
      ai-application-generation-service:
        condition: service_started
      tabular-analysis-service:
        condition: service_started
      ontology-exploratory-analysis-service:
        condition: service_started
      ontology-timeseries-analytics-service:
        condition: service_started
      spreadsheet-computation-service:
        condition: service_started
      analytical-logic-service:
        condition: service_started
      automation-operations-service:
        condition: service_started
      workflow-trace-service:
        condition: service_started
      application-composition-service:
        condition: service_started
      scenario-simulation-service:
        condition: service_started
      solution-design-service:
        condition: service_started
      developer-console-service:
        condition: service_started
      sdk-generation-service:
        condition: service_started
      managed-workspace-service:
        condition: service_started
      custom-endpoints-service:
        condition: service_started
      mcp-orchestration-service:
        condition: service_started
      compute-modules-control-plane-service:
        condition: service_started
      compute-modules-runtime-service:
        condition: service_started
      monitoring-rules-service:
        condition: service_started
      health-check-service:
        condition: service_started
      execution-observability-service:
        condition: service_started
      telemetry-governance-service:
        condition: service_started
      code-security-scanning-service:
        condition: service_started
      code-repository-review-service:
        condition: service_started
    ports:
      - "${OPENFOUNDRY_GATEWAY_HOST_PORT:-8080}:8080"

  web:
    <<: *app-common
    profiles: *profiles-edge
    build:
      context: ..
      dockerfile: apps/web/Dockerfile
    environment:
      NODE_ENV: production
      HOST: 0.0.0.0
      PORT: 3000
    depends_on:
      gateway:
        condition: service_started
    expose:
      - "3000"

  app:
    <<: *app-common
    profiles: *profiles-edge
    image: ${OPENFOUNDRY_NGINX_IMAGE:-nginx:1.27-alpine}
    depends_on:
      web:
        condition: service_started
      gateway:
        condition: service_started
    ports:
      - "${OPENFOUNDRY_APP_HOST_PORT:-3000}:80"
    volumes:
      - ./nginx/openfoundry.conf:/etc/nginx/conf.d/default.conf:ro

volumes:
  postgres_data:
  valkey_data:
  nats_data:
  minio_data:
  vespa_data:
  cassandra_data:
  opensearch_data:
  kafka_data:
  streaming_archive_data:
  event_streaming_archive_data:
  query_data:
  pipeline_data:
  notebook_data:
  report_delivery_data:
  code_repo_data:
