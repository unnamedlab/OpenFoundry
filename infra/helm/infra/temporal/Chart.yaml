apiVersion: v2
name: temporal
description: |
  Temporal cluster (frontend, history, matching, worker-system).
  Persistence backed by Cassandra (see ../cassandra-cluster).
  Wraps the upstream go.temporal.io/helm-charts chart.
type: application
version: 0.1.0
appVersion: "1.24.0"
dependencies:
  - name: temporal
    version: "0.41.0"
    repository: "https://go.temporal.io/helm-charts"
