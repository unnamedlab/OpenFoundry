apiVersion: v2
name: of-ontology
description: |
  OpenFoundry ontology release. Owns ontology definition, actions,
  query, the object database and the ontology indexer sink.
type: application
version: 0.1.0
appVersion: "0.1.0"
dependencies:
  - name: of-shared
    version: 0.1.0
    repository: file://../of-shared
