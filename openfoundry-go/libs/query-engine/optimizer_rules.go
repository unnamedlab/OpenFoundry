// Custom DataFusion optimizer rules.
//
// Future: Row-level security filter injection, cost-based
// optimisations for OpenFoundry-specific table providers.
//
// Mirrors `libs/query-engine/src/optimizer_rules.rs` — kept as a
// doc-only stub on the Go side so the module layout is parallel to
// the Rust crate and future implementations have an obvious home.
package queryengine
