<?xml version="1.0" encoding="utf-8" ?>
<!--
  Minimal Vespa application package used by the integration test in
  `tests/vespa_integration.rs`. It defines a single container cluster that
  exposes both the Document v1 and Search APIs on the default port (8080)
  and a single content cluster with one node so the embedded test runs
  comfortably on a single container.
-->
<services version="1.0">
    <container id="default" version="1.0">
        <document-api/>
        <search/>
        <nodes>
            <node hostalias="node1"/>
        </nodes>
    </container>

    <content id="docs" version="1.0">
        <redundancy>1</redundancy>
        <documents>
            <document type="doc" mode="index"/>
        </documents>
        <nodes>
            <node hostalias="node1" distribution-key="0"/>
        </nodes>
    </content>
</services>
