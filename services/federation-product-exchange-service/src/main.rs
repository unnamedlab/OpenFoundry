// S8 / ADR-0030 (B21) — absorbed crates. Held as dead-code library
// namespaces until the consolidated binary's main is wired in a follow-up.
#[allow(dead_code)]
mod marketplace;
#[allow(dead_code)]
mod marketplace_catalog;
#[allow(dead_code)]
mod product_distribution;

fn main() {}
