apiVersion: v2
name: lakekeeper
description: |
  Lakekeeper Iceberg REST catalog. Wraps the upstream
  lakekeeper/lakekeeper chart and adds OpenFoundry-specific overlays
  (region-B replica, namespace, smoke jobs).
type: application
version: 0.1.0
appVersion: "0.4.0"
dependencies:
  - name: lakekeeper
    version: "0.4.0"
    repository: "https://lakekeeper.github.io/lakekeeper-charts"
