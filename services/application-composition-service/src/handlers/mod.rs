// S8.1.b — `widget-registry-service` consolidated into this service.
// `widgets` exposes the widget catalog and host bridge endpoints
// previously owned by the deleted `widget-registry-service`.

pub mod composition;
pub mod widgets;
