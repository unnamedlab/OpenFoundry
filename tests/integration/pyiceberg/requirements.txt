# PyIceberg integration suite.
#
# Versions pinned for reproducibility — bump in lock-step with the
# `iceberg` Rust crate (libs/storage-abstraction) so the snapshot
# format remains compatible.
pyiceberg[s3fs,pyarrow]==0.7.1
pyarrow==17.0.0
httpx==0.27.0
pytest==8.3.2
