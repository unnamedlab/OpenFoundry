---
# Iceberg catalog backed by Lakekeeper (REST catalog) and the in-cluster
# Ceph RGW S3 endpoint provisioned by Rook (see infra/k8s/rook/).
#
# Lakekeeper is the single Iceberg REST catalog for OpenFoundry per
# docs/architecture/adr/ADR-0008-iceberg-rest-catalog-lakekeeper.md
# (supersedes Apache Polaris). The URL must match the one published in
# infra/k8s/helm/open-foundry/values.yaml (icebergRestCatalog.url) and
# infra/k8s/lakekeeper/values.yaml (LAKEKEEPER__BASE_URI).
#
# The data is stored in the `openfoundry-iceberg` bucket (ObjectBucketClaim
# in infra/k8s/rook/bucket.yaml). S3 credentials are taken from the
# `trino-s3-iceberg` Secret — see runbook for how it is harvested from the
# OBC and rotated.
apiVersion: v1
kind: ConfigMap
metadata:
  name: trino-catalog-iceberg
  namespace: trino
  labels:
    app.kubernetes.io/name: trino
    app.kubernetes.io/component: catalog
    openfoundry.dev/catalog: iceberg
data:
  iceberg.properties: |
    connector.name=iceberg
    iceberg.catalog.type=rest
    iceberg.rest-catalog.uri=http://lakekeeper.lakekeeper.svc:8181
    iceberg.rest-catalog.warehouse=openfoundry
    iceberg.rest-catalog.security=OAUTH2
    iceberg.rest-catalog.oauth2.credential=${ENV:LAKEKEEPER_OAUTH2_CREDENTIAL}
    iceberg.rest-catalog.oauth2.scope=catalog
    iceberg.file-format=PARQUET
    iceberg.compression-codec=ZSTD
    # S3 (Ceph RGW) settings for the underlying storage.
    fs.native-s3.enabled=true
    s3.endpoint=http://rook-ceph-rgw-openfoundry.rook-ceph.svc:80
    s3.region=us-east-1
    s3.path-style-access=true
    s3.aws-access-key=${ENV:S3_ICEBERG_ACCESS_KEY}
    s3.aws-secret-key=${ENV:S3_ICEBERG_SECRET_KEY}
