// Custom DataFusion table providers for OpenFoundry datasets.
//
// Future: Provide table providers that read directly from object
// storage (MinIO/S3) without needing local files.
//
// Mirrors `libs/query-engine/src/datasource.rs` — kept as a
// doc-only stub on the Go side so the module layout is parallel to
// the Rust crate and future implementations have an obvious home.
package queryengine
