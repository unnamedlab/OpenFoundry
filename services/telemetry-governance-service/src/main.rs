// S8 / ADR-0030 (B22) — absorbed crates. Held as dead-code library
// namespaces until the consolidated binary's main is wired in a follow-up.
#[allow(dead_code)]
mod execution_observability;
#[allow(dead_code)]
mod monitoring_rules;

fn main() {}
