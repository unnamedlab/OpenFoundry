---
# Debezium outbox connector for `pg-runtime-config`.
#
# Sources only `outbox.events` from the consolidated runtime/control-plane
# cluster. EventRouter routes to the per-row `topic` value.
apiVersion: kafka.strimzi.io/v1beta2
kind: KafkaConnector
metadata:
  name: outbox-pg-runtime-config
  namespace: kafka
  labels:
    strimzi.io/cluster: debezium
    openfoundry.io/source: pg-runtime-config
  annotations:
    strimzi.io/pause-reconciliation: "false"
spec:
  class: io.debezium.connector.postgresql.PostgresConnector
  tasksMax: 1
  config:
    database.server.name: pg-runtime-config
    topic.prefix: pg-runtime-config
    plugin.name: pgoutput
    slot.name: debezium_outbox_pg_runtime_config
    publication.name: debezium_outbox_pg_runtime_config_pub
    publication.autocreate.mode: filtered
    database.hostname: pg-runtime-config-rw.openfoundry.svc.cluster.local
    database.port: "5432"
    database.user: ${secrets:kafka/pg-runtime-config-debezium:username}
    database.password: ${secrets:kafka/pg-runtime-config-debezium:password}
    database.dbname: app
    schema.include.list: outbox
    table.include.list: outbox.events
    snapshot.mode: never
    transforms: outbox
    transforms.outbox.type: io.debezium.transforms.outbox.EventRouter
    transforms.outbox.route.by.field: topic
    transforms.outbox.route.topic.replacement: ${routedByValue}
    transforms.outbox.table.field.event.id: event_id
    transforms.outbox.table.field.event.key: aggregate_id
    transforms.outbox.table.field.event.payload: payload
    transforms.outbox.table.field.event.payload.id: event_id
    transforms.outbox.table.fields.additional.placement: "headers:header"
    tombstones.on.delete: "false"
    heartbeat.interval.ms: "30000"
    heartbeat.action.query: |
      INSERT INTO outbox.heartbeat (id, last_seen_at)
      VALUES ('debezium-outbox', NOW())
      ON CONFLICT (id) DO UPDATE SET last_seen_at = EXCLUDED.last_seen_at
    producer.override.acks: all
    producer.override.enable.idempotence: "true"
    producer.override.compression.type: zstd
    producer.override.max.in.flight.requests.per.connection: "5"
    producer.override.retries: "2147483647"
    decimal.handling.mode: string
    time.precision.mode: connect
    errors.tolerance: all
    errors.deadletterqueue.topic.name: __dlq.outbox-pg-policy.v1
    errors.deadletterqueue.topic.replication.factor: "3"
    errors.deadletterqueue.context.headers.enable: "true"
    errors.log.enable: "true"
    errors.log.include.messages: "false"
